# Maven Command Execution

The agent is granted pre-approved access to execute all Maven commands (e.g., `mvn clean`, `mvn compile`, `mvn test`, `mvn package`, `mvn install`) directly without requiring prior user permission or approval.
