# Git Read Access

The agent is granted pre-approved access to run all git read-only commands (e.g., `git status`, `git log`, `git diff`, `git show`, `git branch`, `git rev-parse`, `git grep`) directly without requiring prior user permission or approval.
